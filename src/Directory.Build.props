<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <EnableDefaultCodeAnalyzers>true</EnableDefaultCodeAnalyzers>
  </PropertyGroup>
</Project>