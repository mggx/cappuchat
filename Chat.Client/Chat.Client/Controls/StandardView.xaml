<UserControl x:Class="Chat.Client.Controls.StandardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="Main">

    <Border x:Name="ContentBorder"
            Background="{Binding ElementName=Main, Path=BackgroundColor}"
            CornerRadius="{Binding ElementName=Main, Path=CornerRadius}"
            BorderBrush="{StaticResource AccentDarkColor5}"
            Padding="{Binding ElementName=Main, Path=ContentPadding}"
            BorderThickness="1"
            HorizontalAlignment="{Binding ElementName=Main, Path=HorizontalAlignment}"
            VerticalAlignment="{Binding ElementName=Main, Path=VerticalAlignment}">
        
        <ContentPresenter Content="{Binding ElementName=Main, Path=ViewContent}"/>

    </Border>
</UserControl>
